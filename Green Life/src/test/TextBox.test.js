import { render } from "@testing-library/react"
import TextBox from "../components/TextBox"

describe('textBox component', ()=> {
   
    test("la caja de texto se encuentra en el doc", ()=>{
render(<TextBox/>)

    })

})